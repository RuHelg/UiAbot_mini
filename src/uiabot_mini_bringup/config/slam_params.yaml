slam_toolbox:
  ros__parameters:
    use_sim_time: false
    mode: "mapping"            # mapping | localization
    map_frame: "map"
    odom_frame: "odom"
    base_frame: "base_link"
    scan_topic: "scan"         # set to your LaserScan topic name
    odom_topic: "/wheel_encoder_odometry"
    map_update_interval: 1.0
    max_laser_range: 30.0
    # add/tune other parameters as needed (scan matcher, loop closure, etc.)