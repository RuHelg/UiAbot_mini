<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">
  <xacro:macro name="wheel_link" params="name link_mesh wheel_radius joint_origin joint_rpy visual_rpy">
    <joint name="${name}_joint" type="continuous">
      <origin xyz="${joint_origin}" rpy="${joint_rpy}"/>
      <parent link="base_link"/>
      <child link="${name}"/>
      <axis xyz="0 0 1"/>
    </joint>
    <link name="${name}">
      <collision>
        <geometry>
          <sphere radius="${wheel_radius}"/>
        </geometry>
      </collision>
      <visual>
        <origin rpy="${visual_rpy}"/>
        <geometry>
          <mesh filename="${link_mesh}"/>
        </geometry>
        <material name="front_left_wheel_material">
          <color rgba="0.5 0.0 0.5 1.0"/>
        </material>
      </visual>
      <inertial>
        <mass value="0.5"/>
        <inertia ixx="1e-6" ixy="0.0" ixz="0.0" 
                 iyy="1e-6" iyz="0.0" izz="1e-6"/>
      </inertial>
    </link>
  </xacro:macro>
</robot>